{
  "name": "pga-refresh-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that serves the PGA Championship pool dashboard — fetches ESPN's golf leaderboard + pool.json, scores the pool, and serves the combined JSON every minute via edge cache.",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  }
}
